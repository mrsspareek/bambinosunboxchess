import { FullCurriculumExperience } from '../../components/FullCurriculumExperience';

export default function CurriculumLayout({ children: _children }: { children: React.ReactNode }) {
  return <FullCurriculumExperience />;
}
